import { describe, test, beforeEach } from 'vitest'

describe('Product tests', () => {
  //let productStore

  beforeEach(() => {
    /*
    setActivePinia(createPinia())
    productStore = useProductStore()
    productStore.initStore()
    */
  })

  test('Can mark a product OutOfStock as InStock', () => {
    /*
    if (productStore.store.length > 0) {
      console.log(productStore.store) // Affiche le premier produit
    } else {
      console.log('No products found in store')
    }
      */
  })
})
